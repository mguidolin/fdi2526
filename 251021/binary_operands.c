#include <stdio.h>

int main()
{
  int a = -10, b = 3;
  int sum = a + b;
  printf("The result is: %d\n", sum);

  int div = a / b;
  printf("The result is: %d\n", div);

  int mod = a % b;
  printf("The result is: %d\n", mod);

  return 0;
}
