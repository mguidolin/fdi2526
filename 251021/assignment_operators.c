#include <stdio.h>

int main()
{
  int a = 10;
  a += 3; // equivalent to: a = a + 3
  printf("The value of a is: %d\n", a);

  a *= 3; // equivalent to: a = a * 3
  printf("The value of a is: %d\n", a);

  return 0;
}
