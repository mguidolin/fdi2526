#include <stdio.h>

int main()
{
  const int minutes_in_hour = 60;
  printf("One hour has %d minutes.\n", minutes_in_hour);

  const float G = 9.81; // [m/s^2]
  printf("Erath's gravity constant is %.2f m/s^2.\n", G);

  return 0;
}
